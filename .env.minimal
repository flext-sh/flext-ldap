# MINIMAL FLEXT-LDAP CONFIGURATION
# All other settings use smart defaults from FlextConstants
#
# FlextLdapConfig automatically:
# - Detects server capabilities
# - Uses FlextConstants for all timeouts, pools, limits
# - Configures optimal SSL/TLS settings
# - Enables connection pooling with smart defaults
# - Auto-configures retry and caching
#
# ONLY 5 REQUIRED VARIABLES for local testing:

# Connection (field: ldap_port, env_prefix: FLEXT_ → FLEXT_LDAP_PORT)
FLEXT_LDAP_PORT=3390
FLEXT_LDAP_SERVER_URI=ldap://localhost

# Authentication (use empty/remove for anonymous bind)
FLEXT_LDAP_BIND_DN=cn=REDACTED_LDAP_BIND_PASSWORD,dc=flext,dc=local
FLEXT_LDAP_BIND_PASSWORD=REDACTED_LDAP_BIND_PASSWORD123

# Search base (auto-detected from server if omitted)
FLEXT_LDAP_BASE_DN=dc=flext,dc=local

# OPTIONAL - Only if you need to override auto-detection:
# FLEXT_LDAP_USE_SSL=false  # Auto-detected from server URI
# FLEXT_LOG_LEVEL=DEBUG   # Only for debugging (inherited from FlextConfig)

# That's it! FlextConfig handles the rest automatically.
#
# NOTE: Field names have 'ldap_' prefix, so with env_prefix='FLEXT_':
#   ldap_server_uri → FLEXT_LDAP_SERVER_URI (NOT FLEXT_LDAP_LDAP_SERVER_URI)
